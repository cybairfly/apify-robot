const {
    EVENTS,
    LOGGER,
    SERVER,
    TIMEOUTS,
    // TODO remove legacy
    PUPPETEER,
} = require('../../consts');

module.exports = {
    EVENTS,
    LOGGER,
    SERVER,
    TIMEOUTS,
    // TODO remove legacy
    PUPPETEER,
};
